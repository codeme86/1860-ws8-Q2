Inc16(in=out, out=incOut);
Mux16(a=incOut, b=false, sel=reset, out=next);
Register(in=next, load=inc, out=out);

Decoder201890716(
    A=next[0],
    B=next[1],
    C=next[2],
    D=next[3],
    a=a,
    b=b,
    c=c,
    d=d,
    e=e,
    f=f,
    g=g
);