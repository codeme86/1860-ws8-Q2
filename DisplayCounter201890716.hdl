CHIP DisplayCounter201890716 {
    IN inc, reset;
    OUT a, b, c, d, e, f, g;

    PARTS:

    Decoder201890716(A=A, B=B, C=C, D=D, a=a, b=b, c=c, d=d, e=e, f=f, g=g);

    Mux16(a=rev, b=ix, sel=inc, out=fui);
    Mux16(a=fui, b=false, sel=reset, out=next);
    Register(in=next, load=true, out=rev, out[0]=D, out[1]=C, out[2]=B, out[3]=A);
    Inc16(in=rev, out=ix);

}